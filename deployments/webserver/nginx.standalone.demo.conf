# Generated by the SDKWork web server standard (SDKWORK_WEBSERVER_SPEC.md)
# from server.common.toml + server.standalone.toml; do not edit by hand.
user sdkwork;
worker_processes auto;
pid /run/sdkwork/forum/webserver.pid;
error_log /var/log/sdkwork/forum/webserver/error.log warn;
events {
    worker_connections 1024;
}
http {
    sendfile on;
    keepalive_timeout 75;
    client_max_body_size 1100m;
    server_tokens off;
    gzip on;
    include snippets/adaptive-web.maps.conf;
    upstream gateway {
        least_conn;
        keepalive 32;
        server 127.0.0.1:8080;
    }
    server {
        listen 80;
        server_name forum-demo.sdkwork.com forum-demo.birdcoder.com forum-demo.dtupay.com forum-demo.noaper.com forum-demo.sdkwork.cn forum-demo.birdcoder.cn forum-demo.dtupay.cn forum-demo.noaper.cn forum-demo.skubc.com forum-demo.skubc.cn forum-demo.zowalk.com forum-demo.zowalk.cn forum-demo.offer86.com forum-demo.offer86.cn forum-demo.86offer.com forum-demo.86offer.cn;
        include snippets/adaptive-web.named-locations.conf;
        include snippets/gateway-api-locations.nonproduction.conf;
        location / {
            include snippets/adaptive-web.dispatch.conf;
        }
    }
}
