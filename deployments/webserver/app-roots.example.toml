# Adaptive Web roots for sdkwork-forum (copy into runtime config when in-process).
# Docker entrypoint generates /etc/sdkwork/webserver/module-app-roots/sdkwork-forum.toml

[app_roots]
tablet_surface = "pc"

[app_roots.pc_static_by_environment]
development = "apps/sdkwork-forum-pc/dist/standalone/dev"
test = "apps/sdkwork-forum-pc/dist/standalone/test"
staging = "apps/sdkwork-forum-pc/dist/standalone/staging"
production = "apps/sdkwork-forum-pc/dist/standalone/prod"

[app_roots.h5_static_by_environment]
development = "apps/sdkwork-forum-h5/dist/standalone/dev"
test = "apps/sdkwork-forum-h5/dist/standalone/test"
staging = "apps/sdkwork-forum-h5/dist/standalone/staging"
production = "apps/sdkwork-forum-h5/dist/standalone/prod"

[app_roots.static_fallback_by_environment]
development = "deployments/webserver/static"
test = "deployments/webserver/static"
staging = "deployments/webserver/static"
production = "deployments/webserver/static"
